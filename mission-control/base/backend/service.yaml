apiVersion: v1
kind: Service
metadata:
  name: mission-control-backend
  namespace: mission-control
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: mission-control-backend
