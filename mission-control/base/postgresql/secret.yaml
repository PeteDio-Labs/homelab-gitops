apiVersion: v1
kind: Secret
metadata:
  name: postgresql-secret
  namespace: mission-control
type: Opaque
stringData:
  # TODO: Replace with sealed-secret in production
  # Generate with: openssl rand -base64 32
  password: "CHANGE_ME_IN_PROD"
