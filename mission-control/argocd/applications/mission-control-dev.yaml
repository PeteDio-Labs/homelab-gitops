apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mission-control-dev
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: mission-control

  source:
    repoURL: https://github.com/petedillo/homelab-gitops.git
    targetRevision: main
    path: mission-control/overlays/dev

  destination:
    server: https://kubernetes.default.svc
    namespace: mission-control

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
    - CreateNamespace=true
    - PrunePropagationPolicy=foreground
    - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 10
