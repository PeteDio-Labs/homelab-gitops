apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Sealed secrets for mission-control prod environment
# Generated by: ./scripts/generate-sealed-secrets.sh prod
# These are encrypted and safe to commit to Git

resources:
  - backend-secrets.yaml
  - postgresql-secret.yaml
  - nexus-registry.yaml
