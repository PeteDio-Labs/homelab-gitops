apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: observability-stage
  namespace: argocd
spec:
  project: petedillo-stage
  source:
    repoURL: https://github.com/petedillo/homelab-gitops
    targetRevision: HEAD
    path: observability/overlays/stage
  destination:
    server: https://kubernetes.default.svc
    namespace: observability
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
